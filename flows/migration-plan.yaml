name: Data migration plan
description: Plan an online schema/data migration with rollback
input: user_prompt
steps:
  - id: run
    model: { backend: claude, model: claude-opus-4-7 }
    role: planner
    inputs: [user_prompt]
    tools: [Read, Grep, Glob]
    output: plan.md
author: { name: "Overcli", url: "https://github.com/overcodelions/overcli" }
version: 1.0.0
tags: [migration, data, planning, claude]
