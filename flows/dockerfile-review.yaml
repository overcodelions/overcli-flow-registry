name: Dockerfile review
description: Review Dockerfile for size, security, layer cache issues
input: user_prompt
steps:
  - id: run
    model: { backend: claude, model: claude-sonnet-4-6 }
    role: reviewer
    inputs: [user_prompt]
    tools: [Read, Grep]
    output: review.md
author: { name: "Overcli", url: "https://github.com/overcodelions/overcli" }
version: 1.0.0
tags: [review, infra, devops, claude]
